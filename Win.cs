using CI.QuickSave;
using System;
using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;
using UnityEngine.SceneManagement;

public class Win : MonoBehaviour
{/*
    private int Night;
    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void Next()
    {
        Menu.reader.TryRead("Night", out Night);
        Menu.writer.Write("Night", Night + 1).Commit();
        Debug.Log(Night);
        if (Menu.Night == 5)
        {
            Menu.star_1 = true;
        }else if(Menu.Night >= 6)
        {
            Menu.Night = 5;
            Menu.star_2 = true;
        }else if (Menu.custom_night == true)
        {
            Menu.custom_night = false;
            Menu.star_3 = true;
        }
        SceneManager.LoadScene("Menu");
    }*/
}
